#!/usr/bin/env python3
"""
BabelFish Configurator — launch the Textual TUI for profile management.

Standalone app that lists profiles, shows/edits settings, and launches/stops.
Runs independently of the voice assistant (no circular dependency).

Usage:
  ./babelfish-config            # launch the TUI
  ./babelfish-config --list     # list profiles and exit
"""

import sys
import os
from pathlib import Path

_REPO = Path(__file__).resolve().parent
sys.path.insert(0, str(_REPO))

from incarnations.configurator import main

if __name__ == "__main__":
    main()
